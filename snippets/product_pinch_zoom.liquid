{% comment %} ---product_pinch_zoom---
        Do not edit this file.
        This snippet is auto generated and will be overwritten.
        {% endcomment %}
        <script>
        var ai_shop=Shopify.shop;
        var ai_shopStatus=1;
        var ai_zoom_template='{{template}}';
        var ai_zoom_gallerytype=1;
        var ai_zoom_gallerystyle=1;
        var ai_zoom_galleryArrowStyle=1;
        var ai_zoom_plnsts='ZMMagfree19';
        var bc_zoom_magnifier_js ='{{ 'hub_zoom_magnifier_gallery_js.json' | asset_url }}'; 
        var ai_htmlzoommang='';
        var ai_ImageSize='';
          
        {%- assign ai_zoom_product_image_zoom_size = '1300x' -%}     
        {% if ai_zoom_product_image_zoom_size < 600 or ai_zoom_product_image_zoom_size == '' %}
        {%- assign ai_zoom_product_image_zoom_size = '1300x' -%}
        {%endif%} 
        {% if product %}
        if(ai_zoom_gallerytype == 2)  
        { 
          ai_htmlzoommang=`<section id='exemple' style='display:none;'>`;
          {%for  media in product.media %}
            
             {% case media.media_type %}
                {% when 'external_video' %}
                    {% if media.host == 'vimeo' %}
                            ai_htmlzoommang +=`<a href='https://vimeo.com/{{media.external_id}}' class='swipebox-video' rel='vimeo' title='{{media.alt}}'> 
                          </a>`;
            
                    {%else%}  
                         ai_htmlzoommang +=`<a href='https://www.youtube.com/watch?v={{media.external_id}}' class='swipebox-video' rel='youtube' title='{{media.alt}}'> 
                          </a>`;
            
                    {% endif %}
                {% when 'video' %}
                {% else %}
             ai_htmlzoommang +=`<a href='{{media | img_url: ai_zoom_product_image_zoom_size}}' class='swipebox' title='{{media.alt}}'> 
                   <img class='zoomMganifierLoopImg zoomMang_{{ forloop.index }}' src='{{media | img_url: '1x'}}' alt='{{media.alt}}'> </a>`;
                {% endcase %}
            
            {% endfor %}
          ai_htmlzoommang +=`</section>`;
         }
        {% endif %}      
        </script>